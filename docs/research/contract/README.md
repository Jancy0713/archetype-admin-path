# Contract Research

这个目录专门用于沉淀 `contract` 相关的外部调研、内部讨论记录和可复用调研提示词。

当前分层：

- `notes/`：我们内部讨论形成的阶段性结论
- `prompts/`：可直接交给 AI 执行的调研提示词
- `findings/`：按提示词生成出来的调研结果文档

开发推进材料已迁出到：

- [Contract Development Progress](/Users/wangwenjie/project/archetype-admin-path/docs/development_progress/contract/README.md)

这里不再放：

- `contract` 正式流程开发 workplan
- `contract` 正式流程开发 prompt

当前目标：

- 先明确 `contract` 的基础定义
- 再借鉴外部成熟仓库的工作流和结构组织方式
- 最后再决定 `contract` 的正式流程、模板和颗粒度

注意：

- `final_prd` 只是 `contract` 的输入之一，不是 `contract` 流程本身
- 这个目录记录的是“如何设计 `contract` 体系”，不是具体业务 contract 实现
