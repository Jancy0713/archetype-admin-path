# 旧版流程归档

这里存放 `docs/prd` 早期使用的自由 Markdown 流程模板。

这些文件保留的目的只有两个：

1. 作为历史参考
2. 便于回溯旧流程表达方式

当前主流程已经切换到结构化 YAML 产物：

- 主模型直接填写 YAML
- reviewer 直接输出 review YAML
- Markdown 只作为脚本渲染后的展示层

因此，归档目录下的旧模板不应再作为日常执行入口，也不应参与当前主流程 review。
