# Bootstrap Scripts Group

目标承载 `init-07` 的脚本包装层。

范围：

- `bootstrap_plan`
- `project_conventions` 渲染
- `prd_bootstrap_context` 渲染
- `init_execution_scope` 渲染
- `bootstrap_plan` reviewer

当前 wrapper：

- [prepare_bootstrap_plan.rb](/Users/wangwenjie/project/archetype-admin-path/scripts/init/bootstrap/prepare_bootstrap_plan.rb)
